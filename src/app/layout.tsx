import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});


export const metadata: Metadata = {
  title: {
    default: 'Herbs & Wellness | Holistic Integrative Clinic',
    template: '%s | Herbs & Wellness',
  },
  description: 'Holistic integrative clinic offering natural herbal teas, tinctures, capsules, and wellness consultations.',
  icons: {
    icon: '/favicon.jpeg', // or '/favicon.png' / '/icon.svg'
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
